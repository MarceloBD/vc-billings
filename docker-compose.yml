services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: vcbillings
      POSTGRES_PASSWORD: vcbillings
      POSTGRES_DB: vcbillings
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
