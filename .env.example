# Neon Postgres connection string
DATABASE_URL=postgresql://vcbillings:vcbillings@localhost:5432/vcbillings

# bcrypt hash of your chosen password (base64-encoded to avoid $ expansion issues)
# Generate with: node scripts/generate-hash.mjs YOUR_PASSWORD
AUTH_PASSWORD_HASH=JDJiJDEwJHE2L1Z0VnFaTzdMZnNpL0NoVVBzQi5WdlJEQVJjZGJKMXBmVDVGdURNbFFSYU51S2RvNmVH

# Random string for JWT signing (at least 32 characters)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
AUTH_SECRET=your-random-secret-here

# Secret used to authenticate Vercel cron jobs
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
CRON_SECRET=your-cron-secret-here
